# ─────────────────────────────────────────────
#  CTF Server — Environment Variables
#  Copy this to .env and fill in your values
# ─────────────────────────────────────────────

# Admin panel password
ADMIN_PASSWORD=danik2026

# MySQL connection (use your provider's values)
MYSQL_HOST=your-db-host.example.com
MYSQL_PORT=3306
MYSQL_USER=your_db_user
MYSQL_PASSWORD=your_db_password
MYSQL_DATABASE=ctf

# Set to "true" if your MySQL provider requires SSL (PlanetScale, Railway, etc.)
MYSQL_SSL=true

# Server port (hosting providers set this automatically)
PORT=3000
